<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Main App Theme - Dark Mode Adaptive -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        
        <!-- Window Background -->
        <item name="android:windowBackground">#0f172a</item> <!-- Dark Theme Background -->
        
        <!-- Status Bar: Transparent but with LIGHT icons (for dark background) -->
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        
        <!-- Navigation Bar: Transparent but with LIGHT icons -->
        <item name="android:navigationBarColor">#00000000</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o-mr1">false</item>
        
        <!-- Notch support -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
        
        <!-- Full Screen Content -->
        <item name="android:fitsSystemWindows">false</item>
        
        <!-- Translucency Safety -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <!-- Splash Screen Theme - Dark Mode -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0f172a</item>
        <item name="windowSplashScreenIconBackgroundColor">#0f172a</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
</resources>
